// Template literals, also known as template strings, are a feature introduced in ECMAScript 6
// (ES6) that allows for easier and more flexible string formatting in JavaScript.


let name = "deepak";
let output = `${name}`;
console.log(output)
